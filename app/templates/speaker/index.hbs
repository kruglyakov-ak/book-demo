<div class="row row-cols-1 row-cols-md-3">
  {{#each model as |speaker|}}
    {{speaker-item
      speaker=speaker
      deleteAction=(action "deleteSpeaker" speaker)
      tagName=""
    }}
  {{/each}}
</div>